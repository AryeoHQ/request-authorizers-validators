services:
  php:
    image: composer:2
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/tmp/composer
    environment:
      COMPOSER_HOME: /tmp/composer

volumes:
  composer-cache:
